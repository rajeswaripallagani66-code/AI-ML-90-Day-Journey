apiVersion: v1
kind: Service
metadata:
  name: student-report-card-service
spec:
  type: LoadBalancer
  selector:
    app: student-report-card
  ports:
    - name: fastapi
    port: 80
    targetPort: 8000
    - name: streamlit
    port: 8501
    targetPort: 8501